# User Data Folder

Put local KITTI files here when you want the browser viewer to load real data.

Suggested layout:

```text
web_sim/user_data/image_2/000000.png
web_sim/user_data/velodyne/000000.bin
```

These files are ignored by git so the repository does not accidentally commit
large dataset files.
